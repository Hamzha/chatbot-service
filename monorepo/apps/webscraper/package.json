{
  "name": "webscraper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "python -m venv .venv && npm run install:python && npm run install:playwright",
    "dev": ".venv\\Scripts\\python -m uvicorn app.main:app --reload --port 8000",
    "build": "echo No build step for Python service",
    "lint": "echo No JS lint for Python service",
    "check-types": "echo No type-check for Python service",
    "start": ".venv\\Scripts\\python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "install:python": ".venv\\Scripts\\pip install -r requirements.txt",
    "install:playwright": ".venv\\Scripts\\playwright install chromium"
  }
}
