{
  "name": "chatbot-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "python -m venv .venv && npm run install:python",
    "dev": ".venv\\Scripts\\python -m uvicorn app.main:app --reload --port 8001",
    "build": "echo No build step for Python service",
    "lint": "echo No JS lint for Python service",
    "check-types": "echo No type-check for Python service",
    "start": ".venv\\Scripts\\python -m uvicorn app.main:app --host 0.0.0.0 --port 8001",
    "install:python": ".venv\\Scripts\\pip install -e ."
  }
}
